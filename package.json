{
  "name": "friday-blog",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && rm -rf .deploy && git clone --branch gh-pages --single-branch --depth 1 $(git remote get-url origin) .deploy && rsync -a dist/ .deploy/ && touch .deploy/.nojekyll && cd .deploy && git add -A && git diff --cached --quiet || git commit -m 'deploy' && git push origin gh-pages && cd .. && rm -rf .deploy"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.15",
    "astro": "^5.17.1",
    "unist-util-visit": "^5.1.0"
  }
}
